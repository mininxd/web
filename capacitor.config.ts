import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.filetransferapp',
  appName: 'FileTransferApp',
  webDir: 'dist'
};

export default config;
